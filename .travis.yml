language: cpp

sudo: required

dist: trusty

compiler:
    - clang
    - gcc
    
before_script:
    - sudo apt-get install libhdf5-serial-dev hdf5-tools
    
install: true

script: ./scripts/run-tests.sh

notifications:
  email:
    on_success: change
    on_failure: always