language: cpp

sudo: required

dist: trusty

branches:
  only:
    - master
    - stable

compiler:
    - clang
    - gcc
    
before_script:
    - sudo apt-get install libhdf5-serial-dev zlib1g-dev
    
install: true

script: ./scripts/run-tests.sh

notifications:
  slack: icb-dcm:PHyGJ38Zhp8HrwIxjZAwh9ZI
    on_success: change
    on_failure: always